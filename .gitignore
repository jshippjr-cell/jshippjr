# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Editor / OS
.vscode/
.idea/
.DS_Store

# Dashboard SQLite database
chordential.db
*.db

# Generated Veo video clips + assembled film (large binaries; delivered directly)
capabilities_clips/

# Founder-uploaded relevant-work audio samples (local disk; not durable on Render)
src/chordential_oia/web/uploads/

# Third-party agent skills installed via `npx skills add` (kept on local disk,
# not part of the product repo). Those tools run locally, not in CI/prod.
.agents/
.claude/skills/
skills-lock.json
